extends layout


block content
    br
    h1 #{title}
    form(method='POST', action='/add_blocks')
        #form-group
            label Name:
            input.form-control(name='name', type='text')
        br
        #form-group
            label Content:
            textarea.form-control(name='content', rows=10)
        br
        input.btn.btn-primary(type='submit', value='Submit')